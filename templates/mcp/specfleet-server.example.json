{
  "$schema": "https://example.com/specfleet/mcp-manifest.schema.json",
  "name": "specfleet-context",
  "displayName": "SpecFleet shared org context",
  "description": "Stdio MCP server that exposes .specfleet/ (instruction, decisions, project, charters, audit) as tools and resources so any Copilot CLI or VS Code agent can read shared org context without re-loading files.",
  "command": "specfleet",
  "args": ["mcp", "serve"],
  "scope": "workspace",
  "_copilotCliExample": {
    "comment": "Drop the block below into ~/.copilot/mcp-config.json (or your project's .mcp.json) to wire up @specfleet-context as an MCP server.",
    "mcpServers": {
      "specfleet-context": {
        "command": "specfleet",
        "args": ["mcp", "serve"]
      }
    }
  },
  "_vscodeExample": {
    "comment": "VS Code MCP block (settings.json under chat.mcp.servers).",
    "specfleet-context": {
      "command": "specfleet",
      "args": ["mcp", "serve"],
      "env": {}
    }
  }
}
