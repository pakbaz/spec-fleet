{
  "//": "EAS egress allowlist. Comments-as-keys (// or #) are stripped at load.",
  "//doc": "Hosts can be exact ('api.github.com'), with port ('api.github.com:443'), or subdomain wildcards ('*.github.com'). Empty 'allow' = deny-all.",
  "allow": [
    "api.github.com",
    "*.github.com",
    "objects.githubusercontent.com",
    "registry.npmjs.org",
    "*.azure.com"
  ],
  "deny": [
    "*.suspicious-domain.example"
  ]
}
